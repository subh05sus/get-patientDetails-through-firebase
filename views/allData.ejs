<!DOCTYPE html>
<html>

<head>
    <title>User Details</title>
    <style>
        /* Add your CSS styles here */
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: start;
            margin-top: 20px;
            min-height: 100vh;
        }

        .card {
            background-color: #fff;
            border-radius: 10px;
            border-color: #444444;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            padding: 20px;
            width: 90%;
            max-height: 90%;
            text-align: left;
        }

        h1 {
            color: #333;
        }

        p {
            color: #555;
            margin: 8px 0;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            margin-bottom: 20px;
            border-radius: 10px;
            padding: 10px;
            background-color: #fff;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
        }

        button {
            background-color: #072AC8;
            color: #fff;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 5px;
        }

        button:hover {
            background-color: #001c94;
        }

        @media (max-width: 1080px) {
            .card {
                padding: 45px;
            }

            h1 {
                font-size: 72px;
            }

            h2,
            h3 {
                font-size: 60px;
            }

            p {
                font-size: 48px;
                margin: 36px 0;
            }

            ul {
                margin-top: 36px;
            }

            li {
                padding: 36px;
                margin-bottom: 60px;
            }

            button {
                padding: 42px 84px;
                font-size: 60px;
            }
        }
    </style>
</head>

<body>
    <div class="card">

        <% for (let i=0; i < allPatientDataToBeRendered.length; i++) { %>
            <li>
                <h3><%= allPatientDataToBeRendered[i].firstName %>
                        <%= allPatientDataToBeRendered[i].lastName %>
                </h3>
                <span><b>Sex:</b> <%= allPatientDataToBeRendered[i].sex %>
                </span> 
                <span><b>Age:</b> <%= allPatientDataToBeRendered[i].age %>
                </span> 
                <span><b>Phone Number:</b> <%= allPatientDataToBeRendered[i].phone %>
                </span> 
                <span><b>Email:</b> <%= allPatientDataToBeRendered[i].email %>
                </span>

                <p><b>Summary:</b> <%= allPatientDataToBeRendered[i].patientBasicData %>
                </p>
            </li>
            <% } %>

                <button onclick="goBack()">Back</button>
    </div>

    <script>
        function goBack() {
            window.history.back();
        }
    </script>
</body>

</html>