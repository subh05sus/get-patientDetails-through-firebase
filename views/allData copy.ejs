<!DOCTYPE html>
<html>

<head>
    <title>User Details</title>
    <style>
        /* Add your CSS styles here */
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: start;
            margin-top: 20px;
            min-height: 100vh;
            list-style-type: none;

        }
        .popup {
            display: none;
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background-color: #fff;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3);
            padding: 20px;
            max-width: 400px;
            z-index: 9999;
        }

        .card {
            background-color: #fff;
            border-radius: 10px;
            border-color: #444444;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            padding: 20px;
            width: 90%;
            max-height: 90%;
            text-align: left;
        }

        h1 {
            color: #333;
        }

        p {
            color: #555;
            margin: 8px 0;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            margin-bottom: 20px;
            border-radius: 10px;
            padding: 10px;
            background-color: #fff;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
        }

        button {
            background-color: #072AC8;
            color: #fff;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 5px;
        }

        button:hover {
            background-color: #001c94;
        }

        @media (max-width: 1080px) {
            .card {
                padding: 45px;
            }

            h1 {
                font-size: 72px;
            }

            h2,
            h3 {
                font-size: 60px;
            }

            p {
                font-size: 48px;
                margin: 36px 0;
            }

            ul {
                margin-top: 36px;
            }

            li {
                padding: 36px;
                margin-bottom: 60px;
            }

            button {
                padding: 42px 84px;
                font-size: 60px;
            }
        }
    </style>
</head>

<body>
    <div class="card">
        <h1>All Patients</h1>
        <% for (let i=0; i < allPatientDataToBeRendered.length; i++) { %>
            <li>
                <h3 onclick="showPopup('<%= allPatientDataToBeRendered[i].firstName %> <%= allPatientDataToBeRendered[i].lastName %><p>Sex: <%= allPatientDataToBeRendered[i].sex %></p><p>Age: <%= allPatientDataToBeRendered[i].age %></p><p>Phone Number: <%= allPatientDataToBeRendered[i].phone %></p><p>Email: <%= allPatientDataToBeRendered[i].email %></p><p>Height: <%= allPatientDataToBeRendered[i].height %></p><p>Weight: <%= allPatientDataToBeRendered[i].weight %></p><p>Basic Data Given: <%= allPatientDataToBeRendered[i].firstData %></p><p>Summary: <%= allPatientDataToBeRendered[i].patientBasicData %></p>')">
                    <%= allPatientDataToBeRendered[i].firstName %>
                        <%= allPatientDataToBeRendered[i].lastName %>
                </h3>
                <span><b>Sex:</b>
                    <%= allPatientDataToBeRendered[i].sex %>
                </span>
                <span><b>Age:</b>
                    <%= allPatientDataToBeRendered[i].age %>
                </span>
                <span><b>Phone Number:</b>
                    <%= allPatientDataToBeRendered[i].phone %>
                </span>
                <span><b>Email:</b>
                    <%= allPatientDataToBeRendered[i].email %>
                </span>

                <p><b>Summary:</b>
                    <%= allPatientDataToBeRendered[i].patientBasicData %>
                </p>
            </li>
            <% } %>

                <button onclick="goBack()">Back</button>
    </div>
     <!-- Hidden popup and overlay -->
     <div class="overlay" id="overlay"></div>
     <div class="popup" id="popup">
         <h2>Patient Details</h2>
         <p id="patientDetails"></p>
         <button onclick="closePopup()">Close</button>
     </div>

    <script>
        function goBack() {
            window.history.back();
        }
        // function detailedView(index) {
            
        //     const patient = allPatientDataToBeRendered[index];
        //     console.log(patient)
        //     // Check if patient exists
        //     if (patient) {
        //         const popupWindow = window.open("", "Patient Details", "width=400,height=400");

        //         const popupContent = `
        //             <h2>Patient Details</h2>
        //             <p><b>Name:</b> ${patient.firstName} ${patient.lastName}</p>
        //             <p><b>Sex:</b> ${patient.sex}</p>
        //             <p><b>Age:</b> ${patient.age}</p>
        //             <p><b>Phone Number:</b> ${patient.phone}</p>
        //             <p><b>Email:</b> ${patient.email}</p>
        //             <p><b>Summary:</b> ${patient.patientBasicData}</p>
        //         `;

        //         popupWindow.document.body.innerHTML = popupContent;
        //     }
        // }

        function showPopup(patientName) {
            const popup = document.getElementById("popup");
            const overlay = document.getElementById("overlay");
            const patientDetails = document.getElementById("patientDetails");
            
            // Set the patient details in the popup
            patientDetails.textContent = "Name: " + patientName; // You can add more details here

            // Show the popup and overlay
            popup.style.display = "block";
            overlay.style.display = "block";
        }

        function closePopup() {
            const popup = document.getElementById("popup");
            const overlay = document.getElementById("overlay");
            
            // Hide the popup and overlay
            popup.style.display = "none";
            overlay.style.display = "none";
        }
    </script>
</body>

</html>